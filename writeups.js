/* این فایل داده‌ی منتشرشده‌ی سایت است.
   از پنل مدیریت با دکمه‌ی «انتشار روی سایت» دانلود و اینجا جایگزین می‌شود. */
window.WRITEUPS = [
  {
    "id": "ms8lrjcbqle8",
    "title": "جزوه‌ی Bash و خط فرمان لینوکس",
    "tag": "",
    "date": "۹ مرداد ۱۴۰۵",
    "excerpt": "جزوه‌ی مرور مفاهیم پایه‌ی شل: جریان‌های ورودی/خروجی، هدایت داده، عملگرها، مجوزها، پردازش متن، شرط‌ها، حلقه‌ها و ابزارهای جانبی. ۱.…",
    "body": "جزوه‌ی مرور مفاهیم پایه‌ی شل: جریان‌های ورودی/خروجی، هدایت داده، عملگرها، مجوزها، پردازش متن، شرط‌ها، حلقه‌ها و ابزارهای جانبی.\n\n---\n\n## ۱. مفاهیم پایه\n\n| مفهوم | توضیح |\n| --- | --- |\n| Shell | برنامه‌ای که دستورات متنی را می‌گیرد و به سیستم‌عامل تحویل می‌دهد |\n| Bash | رایج‌ترین شل در لینوکس |\n| Command | یک برنامه‌ی اجرایی که با نام صدا زده می‌شود |\n| Argument | داده‌ای که بعد از نام دستور نوشته می‌شود |\n| Option / Flag | تغییردهنده‌ی رفتار دستور، معمولاً با `-` یا `--` |\n| Script | فایلی متنی حاوی چند دستور که پشت سر هم اجرا می‌شوند |\n\n> نکته: شل قبل از اجرای دستور، خط را بازنویسی می‌کند (متغیرها، `*`، `$( )` را جایگزین می‌کند) و بعد نتیجه را اجرا می‌کند.\n> نکته: ترتیب پردازش همیشه از چپ به راست است؛ در چند جا این ترتیب نتیجه را عوض می‌کند.\n\n---\n\n## ۲. IO Streams\n\nهر برنامه در لینوکس سه مسیر ارتباطی با بیرون دارد. هرکدام یک شماره دارند که به آن **File Descriptor** می‌گویند.\n\n| شماره | نام | نقش |\n| --- | --- | --- |\n| `0` | stdin | ورودی برنامه |\n| `1` | stdout | خروجی موفق |\n| `2` | stderr | پیام خطا |\n\n`stdout` و `stderr` دو مسیر کاملاً جدا هستند، اما به‌طور پیش‌فرض هر دو روی صفحه‌ی ترمینال نمایش داده می‌شوند. به همین دلیل با چشم قابل تفکیک نیستند، ولی لینوکس آن‌ها را جدا می‌شناسد.\n\n### مثال برای هر جریان\n\n```bash\nwhoami                 # stdout : خروجی موفق\nls missing-file.txt    # stderr : پیام خطا\nwc -l                  # stdin  : تایپ کن، سپس Ctrl+D\n```\n\n> نکته: وقتی یک دستور منتظر می‌ماند و چیزی چاپ نمی‌کند، معمولاً منتظر stdin است. با `Ctrl+D` به آن می‌گویی ورودی تمام شد.\n\n### خلاصه این بخش\n\nسه جریان مستقل داریم؛ هر عملیات هدایت داده در ادامه‌ی جزوه فقط یعنی «یکی از این سه شماره را به جای دیگری وصل کن».\n\n---\n\n## ۳. Redirection\n\nالگوی کلی همیشه ثابت است:\n\n```text\nشماره‌ی جریان + علامت + مقصد\n```\n\n| نوشتار | عملکرد |\n| --- | --- |\n| `>` | stdout به فایل، با بازنویسی |\n| `1>` | همان `>` با شماره‌ی صریح |\n| `>>` | stdout به انتهای فایل |\n| `<` | خواندن stdin از فایل |\n| `2>` | stderr به فایل، با بازنویسی |\n| `2>>` | stderr به انتهای فایل |\n| `2>&1` | stderr به مقصد فعلی stdout |\n| `&>` | هر دو خروجی به یک فایل |\n| `2>/dev/null` | دور ریختن خطاها |\n| `<< WORD` | Heredoc، ورودی چندخطی |\n\n> نکته: `>` در واقع مخفف `1>` است. عدد ۱ چون رایج‌ترین حالت است حذف می‌شود، اما عدد ۲ باید نوشته شود.\n> نکته: `<<` عملگر append نیست. append با `>>` انجام می‌شود و `<<` مربوط به Heredoc است.\n> نکته: `<` فقط برای خواندن است و هرگز چیزی در فایل نمی‌نویسد.\n\n### بازنویسی در برابر افزودن\n\n```bash\nwhoami > log.txt     # فایل را از صفر می‌نویسد\ndate  >> log.txt     # به انتهای فایل اضافه می‌کند\n```\n\n> نکته: `>` محتوای قبلی فایل را بدون هشدار پاک می‌کند و این عمل برگشت‌پذیر نیست.\n\n### تفکیک stdout از stderr\n\n```bash\ntouch real.txt\n\nls real.txt fake.txt                          # هر دو روی صفحه\nls real.txt fake.txt > good.txt               # فقط خطا روی صفحه می‌ماند\nls real.txt fake.txt 2> err.txt               # فقط نتیجه‌ی سالم روی صفحه می‌ماند\nls real.txt fake.txt > good.txt 2> err.txt    # صفحه خالی می‌ماند\n```\n\n### ترکیب هر دو خروجی\n\n```bash\nls real.txt fake.txt > all.log 2>&1     # درست\nls real.txt fake.txt &> all.log         # میان‌بُر\nls real.txt fake.txt 2>&1 > all.log     # اشتباه\n```\n\nدلیل اهمیت ترتیب: خط از چپ به راست پردازش می‌شود. در حالت اشتباه، لحظه‌ی اجرای `2>&1` هنوز جریان ۱ به ترمینال وصل است، پس stderr هم به ترمینال می‌رود نه به فایل.\n\n> نکته: در `2>&1` علامت `&` ضروری است. `&1` یعنی «جریان شماره ۱». بدون آن، `2>1` خطاها را در فایلی به نام `1` می‌ریزد.\n\n### دور ریختن خروجی\n\n```bash\nfind / -name \"passwd\" 2>/dev/null\n```\n\n`/dev/null` یک مقصد پوچ است؛ هرچه در آن ریخته شود از بین می‌رود.\n\n### Heredoc\n\n```bash\ncat << EOF > script.sh\necho \"line one\"\necho \"line two\"\nEOF\n```\n\nکلمه‌ی `EOF` دلخواه است؛ فقط باید ابتدا و انتها یکسان باشند.\n\n> نکته: اگر نمی‌خواهی متغیرها همان لحظه جایگزین شوند، از `<< 'EOF'` با کوتیشن استفاده کن.\n\n### تفاوت آرگومان با ورودی هدایت‌شده\n\n```bash\nwc -l report.txt      # خروجی : 2 report.txt\nwc -l < report.txt    # خروجی : 2\n```\n\nدر حالت اول، خودِ برنامه فایل را باز می‌کند و نام آن را می‌داند. در حالت دوم، شل فایل را باز می‌کند و برنامه فقط یک متن بی‌نام دریافت می‌کند.\n\n### خلاصه این بخش\n\n| می‌خواهی | بنویس |\n| --- | --- |\n| نتیجه در فایل ذخیره شود | `>` |\n| به فایل قبلی اضافه شود | `>>` |\n| فقط خطاها ذخیره شوند | `2>` |\n| همه‌چیز در یک فایل | `&> file` |\n| خطاها نادیده گرفته شوند | `2>/dev/null` |\n| فایل به‌جای صفحه‌کلید ورودی باشد | `<` |\n\n---\n\n## ۴. Pipe\n\nقاعده‌ی `|` در یک جمله: **stdout** دستور سمت چپ به **stdin** دستور سمت راست تحویل داده می‌شود.\n\n```bash\nls | wc -l\ncat /etc/passwd | grep bash\nid | base64\n```\n\n### زنجیره کردن\n\n```bash\ncat /etc/passwd | grep bash | cut -d \":\" -f 1 | head -n 5\n```\n\n1. `cat` تمام فایل را می‌دهد\n2. `grep` فیلتر می‌کند\n3. `cut` ستون اول را جدا می‌کند\n4. `head` پنج مورد اول را نگه می‌دارد\n\n### دو محدودیت مهم\n\n**۱. pipe فقط stdout را منتقل می‌کند، نه stderr.**\n\n```bash\nls real.txt fake.txt | wc -l\n# پیام خطا همچنان روی صفحه ظاهر می‌شود\n```\n\n**۲. دستور سمت راست باید stdin بپذیرد.**\n\n```bash\nid | base64                # کار می‌کند\necho \"google.com\" | dig    # کار نمی‌کند؛ dig از stdin نمی‌خواند\ndig google.com             # درست\n```\n\n> نکته: اگر برنامه‌ای stdin نمی‌پذیرد ولی می‌خواهی خروجی را به آن بدهی، از `xargs` استفاده کن.\n\n### تفاوت `|` با `>`\n\n| نماد | مقصد |\n| --- | --- |\n| `>` | یک فایل |\n| `\\|` | یک دستور دیگر |\n\n---\n\n## ۵. Command Separators و Operators\n\n### Exit Status\n\nهر دستور پس از پایان یک عدد اعلام می‌کند:\n\n| مقدار | معنی |\n| --- | --- |\n| `0` | موفق |\n| غیرصفر | ناموفق |\n\n```bash\nwhoami\necho $?      # 0\n\nls fake.txt\necho $?      # 2\n```\n\n> نکته: این قاعده برعکس اکثر زبان‌های برنامه‌نویسی است که در آن‌ها `0` معادل `false` است.\n\n### جدول عملگرها\n\n| نماد | نام | دستور دوم چه زمانی اجرا می‌شود؟ |\n| --- | --- | --- |\n| `;` | Separator | همیشه، بدون شرط |\n| `&&` | AND | فقط اگر اولی موفق شود |\n| `\\|\\|` | OR | فقط اگر اولی ناموفق شود |\n| `&` | Background | دستور را رها می‌کند و ترمینال آزاد می‌ماند |\n\n```bash\nid ; whoami                      # هر دو، مستقل از هم\nid && whoami                     # دومی فقط در صورت موفقیت اولی\nls fake.txt || echo \"not found\"  # دومی فقط در صورت شکست اولی\n```\n\n### شباهت با Short-Circuit در برنامه‌نویسی\n\n| | برنامه‌نویسی | Bash |\n| --- | --- | --- |\n| موضوع ارزیابی | یک مقایسه | یک دستور واقعی که اجرا می‌شود |\n| معنی «درست» | `true` | exit status صفر |\n\n### الگوی شرطی فشرده\n\n```bash\n[ -f report.txt ] && cat report.txt || echo \"not found\"\n```\n\n> نکته: در `a && b || c`، اگر `b` شکست بخورد `c` هم اجرا می‌شود. این دقیقاً معادل `if/else` نیست؛ برای منطق پیچیده از `if` واقعی استفاده کن.\n\n### اجرای پس‌زمینه\n\n```bash\nsleep 30      # ترمینال قفل می‌شود\nsleep 30 &    # ترمینال آزاد می‌ماند\n```\n\nخروجی `&` چیزی شبیه `[1] 4823` است: `[1]` شماره‌ی job و `4823` شماره‌ی پروسه.\n\n```bash\njobs      # فهرست کارهای پس‌زمینه\nfg        # بازگرداندن به پیش‌زمینه\nbg        # ادامه‌ی اجرا در پس‌زمینه\n```\n\n> نکته: `&` تکی و `&&` دوتایی هیچ ارتباطی با هم ندارند.\n\n---\n\n## ۶. Permissions\n\n### خواندن خروجی `ls -l`\n\n```text\n-rwxr-xr--  1  zetsu  zetsu  45  Jul 29 14:44  script.sh\n│└┬┘└┬┘└┬┘        │      │\n│ │  │  │         │      └── گروه\n│ │  │  │         └───────── مالک\n│ │  │  └── others\n│ │  └───── group\n│ └──────── user (owner)\n└────────── نوع : `-` فایل ، `d` پوشه\n```\n\n### سه مجوز و ارزش عددی\n\n| حرف | نام | ارزش | معنی روی فایل |\n| --- | --- | --- | --- |\n| `r` | read | 4 | خواندن محتوا |\n| `w` | write | 2 | تغییر محتوا |\n| `x` | execute | 1 | اجرا کردن |\n\n### تبدیل رشته به عدد\n\n| رشته | محاسبه | عدد |\n| --- | --- | --- |\n| `rwx` | 4+2+1 | 7 |\n| `rw-` | 4+2 | 6 |\n| `r-x` | 4+1 | 5 |\n| `r--` | 4 | 4 |\n| `---` | 0 | 0 |\n\nمثال: `-rw-r-xr--` یعنی `rw-` = ۶، `r-x` = ۵، `r--` = ۴، پس عدد نهایی **654** است.\n\n### دستورات\n\n```bash\nchmod +x script.sh      # افزودن مجوز اجرا\nchmod 744 script.sh     # کنترل دقیق سه دسته\nchmod 700 secret.txt    # فقط مالک\n```\n\n### اعداد پرکاربرد\n\n| عدد | رشته | کاربرد |\n| --- | --- | --- |\n| `755` | `rwxr-xr-x` | اسکریپت‌ها و برنامه‌های سیستمی |\n| `750` | `rwxr-x---` | اسکریپت گروهی |\n| `700` | `rwx------` | فایل خصوصی قابل اجرا |\n| `644` | `rw-r--r--` | فایل متنی معمولی |\n| `600` | `rw-------` | فایل حساس مثل کلید SSH |\n\n### مالکیت در برابر مجوز\n\n| عملیات | کنترل‌کننده |\n| --- | --- |\n| خواندن، نوشتن، اجرا | مجوزها (`r`, `w`, `x`) |\n| تغییر دادن خود مجوزها | مالکیت |\n\n> نکته: داشتن مجوز `w` به معنی اجازه‌ی زدن `chmod` نیست. فقط مالک فایل یا `root` می‌تواند مجوزها را تغییر دهد.\n> نکته: برای اجرای یک اسکریپت، مجوز `x` لازم است؛ `w` فقط اجازه‌ی ویرایش می‌دهد.\n\n---\n\n## ۷. ابزارهای پردازش متن\n\nهمه‌ی این ابزارها هم آرگومان می‌پذیرند و هم stdin.\n\n### wc\n\n```bash\nwc -l file.txt    # تعداد خط\nwc -w file.txt    # تعداد کلمه\nwc -c file.txt    # تعداد بایت\n```\n\n### head و tail\n\n```bash\nhead file.txt              # ۱۰ خط اول\nhead -n 3 file.txt         # ۳ خط اول\ntail -n 3 file.txt         # ۳ خط آخر\ntail -f /var/log/syslog    # نمایش زنده‌ی خطوط جدید\n```\n\n### grep\n\n| فلگ | کاربرد |\n| --- | --- |\n| `-i` | بدون حساسیت به بزرگی و کوچکی حروف |\n| `-v` | معکوس؛ خطوطی که شامل الگو نیستند |\n| `-c` | فقط تعداد خطوط منطبق |\n| `-n` | نمایش شماره‌ی خط |\n| `-r` | جست‌وجو در تمام زیرپوشه‌ها |\n\n```bash\ngrep bash users.txt\ngrep -i ROOT users.txt\ngrep -v bash users.txt | wc -l\ngrep -c bash users.txt\ngrep \"^root\" users.txt\n```\n\n### cut\n\n`cut` مثل قیچی است: خط را از روی یک جداکننده می‌بُرد و ستون دلخواه را برمی‌دارد.\n\n| فلگ | کاربرد |\n| --- | --- |\n| `-d` | جداکننده؛ پیش‌فرض Tab است |\n| `-f` | شماره‌ی ستون، از ۱ شروع می‌شود |\n\n```bash\necho \"ali,reza,sara\" | cut -d \",\" -f 2      # reza\ncut -d \":\" -f 1 /etc/passwd                 # نام کاربران\ncut -d \":\" -f 1,7 /etc/passwd               # ستون ۱ و ۷\ncut -d \":\" -f 1-3 /etc/passwd               # ستون ۱ تا ۳\n```\n\nساختار `/etc/passwd`:\n\n```text\nroot:x:0:0:root:/root:/bin/bash\n 1   2 3 4  5    6       7\n```\n\n| ستون | محتوا |\n| --- | --- |\n| ۱ | نام کاربری |\n| ۲ | `x` |\n| ۳ | UID |\n| ۴ | GID |\n| ۵ | توضیح |\n| ۶ | مسیر خانه |\n| ۷ | شل پیش‌فرض |\n\n### sed\n\n```bash\nsed 's/a/m/'  file.txt      # اولین a در هر خط\nsed 's/a/m/g' file.txt      # همه‌ی a ها\nsed 's/[0-9]//g' file.txt   # حذف تمام ارقام\nsed 's|/home|/data|g' f.txt # جداکننده لازم نیست / باشد\n```\n\n> نکته: `sed` به‌طور پیش‌فرض فایل را تغییر نمی‌دهد و فقط نتیجه را چاپ می‌کند. `-i` فایل را واقعاً بازنویسی می‌کند و برگشت‌پذیر نیست.\n\n### tee\n\nهم‌زمان در فایل می‌نویسد و خروجی را به مرحله‌ی بعد پاس می‌دهد.\n\n```bash\nwhoami | tee -a output.txt\ncat users.txt | tee backup.txt | grep bash\n```\n\n### خلاصه این بخش\n\n| ابزار | کار |\n| --- | --- |\n| `wc` | شمردن |\n| `head` / `tail` | برش از ابتدا یا انتها |\n| `grep` | فیلتر کردن خطوط |\n| `cut` | جدا کردن ستون |\n| `sed` | جایگزینی متن |\n| `tee` | ذخیره در مسیر عبور داده |\n\n---\n\n## ۸. Regex پایه\n\n| نماد | معنی |\n| --- | --- |\n| `^` | ابتدای خط |\n| `$` | انتهای خط |\n| `.` | هر کاراکتر، دقیقاً یکی |\n| `[0-9]` | یک رقم |\n| `[a-z]` | یک حرف کوچک |\n| `*` | تکرار صفر بار یا بیشتر |\n| `\\|` | یا |\n\n```bash\ngrep \"root\"     users.txt   # root در هر جای خط\ngrep \"^root\"    users.txt   # خطوطی که با root شروع می‌شوند\ngrep \"bash$\"    users.txt   # خطوطی که به bash ختم می‌شوند\ngrep \"[0-9]\"    users.txt   # خطوط دارای رقم\n```\n\n> نکته: `*` در Regex با `*` در شل فرق دارد. در شل `*` نام فایل‌ها را جایگزین می‌کند، در Regex به معنی تکرار است.\n\n---\n\n## ۹. متغیرها و Command Substitution\n\n### متغیر\n\n```bash\nname=\"ashkan\"\necho \"hello $name\"\n```\n\n> نکته: دور `=` نباید فاصله باشد. `name = \"ashkan\"` خطا می‌دهد چون شل `name` را نام یک دستور فرض می‌کند.\n> نکته: هنگام مقداردهی `$` نمی‌گذاریم؛ `$` فقط هنگام خواندن مقدار استفاده می‌شود.\n\n### Command Substitution\n\nخروجی یک دستور را به یک مقدار متنی تبدیل می‌کند.\n\n```bash\necho \"user is $(whoami)\"\ncount=$(ls | wc -l)\necho \"this folder has $count files\"\n```\n\n| نوشتار | وضعیت |\n| --- | --- |\n| `$(command)` | روش استاندارد و توصیه‌شده |\n| `` `command` `` | روش قدیمی، تودرتو کردنش دشوار است |\n\n### تفاوت انواع پرانتز\n\n| نوشتار | نام | کار |\n| --- | --- | --- |\n| `$( )` | Command Substitution | خروجی دستور را برمی‌گرداند |\n| `$(( ))` | Arithmetic Expansion | محاسبه‌ی ریاضی |\n| `( )` | Subshell | اجرای دستورات در یک شل موقت |\n\n### محاسبات\n\n```bash\necho $(( 5 * 3 ))       # 15\necho $(( 10 / 2 + 1 ))  # 6\n```\n\n> نکته: `echo 5 * 3` عدد چاپ نمی‌کند؛ شل `*` را به نام فایل‌های پوشه تبدیل می‌کند. یا کوتیشن بگذار یا از `$(( ))` استفاده کن.\n\n### Subshell\n\n```bash\npwd            # /home/zetsu\n( cd /tmp ; pwd )   # /tmp\npwd            # /home/zetsu\n```\n\nتغییرات داخل پرانتز فقط در همان شل موقت اعتبار دارند و روی شل اصلی اثر نمی‌گذارند.\n\n### Snapshot در برابر لحظه‌ای\n\n```bash\nnow=$(date)      # زمان همان لحظه ذخیره می‌شود\necho \"$now\"      # همیشه همان مقدار\necho \"$(date)\"   # هر بار زمان جدید\n```\n\n---\n\n## ۱۰. شرط‌ها\n\n### ساختار پایه\n\n```bash\nif command\nthen\n    another_command\nfi\n```\n\n`if` به **exit status** نگاه می‌کند، نه به متن خروجی. کلمه‌ی `fi` همان `if` برعکس است و پایان بلوک را نشان می‌دهد.\n\n```bash\nif whoami\nthen\n    echo \"whoami worked\"\nfi\n```\n\n### فرم یک‌خطی\n\n```bash\nif whoami ; then echo \"ok\" ; fi\n```\n\n### else و elif\n\n```bash\nif [ -f report.txt ]\nthen\n    echo \"file exists\"\nelif [ -d report.txt ]\nthen\n    echo \"it is a directory\"\nelse\n    echo \"nothing found\"\nfi\n```\n\n### دستور `[ ]`\n\n`[` یک دستور واقعی است، معادل `test`. به همین دلیل فاصله‌ها اجباری‌اند.\n\n```bash\n[ -f file.txt ]      # درست\n[-f file.txt]        # خطا\n```\n\n#### تست فایل و پوشه\n\n| تست | درست است اگر |\n| --- | --- |\n| `-f` | فایل معمولی باشد |\n| `-d` | پوشه باشد |\n| `-e` | اصلاً وجود داشته باشد |\n| `-r` | قابل خواندن باشد |\n| `-w` | قابل نوشتن باشد |\n| `-x` | قابل اجرا باشد |\n| `-s` | خالی نباشد |\n\n#### مقایسه‌ی عددی\n\n| نماد | معنی |\n| --- | --- |\n| `-eq` | برابر |\n| `-ne` | نابرابر |\n| `-gt` | بزرگ‌تر |\n| `-lt` | کوچک‌تر |\n| `-ge` | بزرگ‌تر یا مساوی |\n| `-le` | کوچک‌تر یا مساوی |\n\n#### مقایسه‌ی رشته\n\n| نماد | معنی |\n| --- | --- |\n| `=` | برابر |\n| `!=` | نابرابر |\n| `-z` | خالی بودن رشته |\n| `-n` | خالی نبودن رشته |\n\n> نکته: برای مقایسه‌ی عددی از `>` و `<` استفاده نکن؛ شل آن‌ها را redirection می‌فهمد و فایل می‌سازد.\n> نکته: بعد از `if` پرانتز نگذار. `if (pwd)` یک subshell بی‌دلیل می‌سازد؛ فرم درست `if pwd` است.\n> نکته: کوتیشن فارسی «‏”‏» معتبر نیست؛ فقط از `\"` استفاده کن.\n\n### الگوی پرکاربرد\n\n```bash\nif [ ! -d backup ]\nthen\n    mkdir backup\n    echo \"backup created\"\nfi\n```\n\n---\n\n## ۱۱. حلقه‌ها\n\n### for\n\n```bash\nfor animal in cat dog bird\ndo\n    echo \"animal : $animal\"\ndone\n```\n\n> نکته: کلماتی که بعد از `in` می‌آیند داده هستند، نه دستور. `cat` اینجا فقط یک رشته است.\n\n#### منابع مختلف برای لیست\n\n```bash\nfor i in 1 2 3         ; do echo \"$i\" ; done\nfor i in {1..5}        ; do echo \"$i\" ; done\nfor i in {5..1}        ; do echo \"$i\" ; done\nfor i in $(seq 1 5)    ; do echo \"$i\" ; done\nfor f in *.txt         ; do echo \"$f\" ; done\n```\n\n#### محاسبه داخل حلقه\n\n```bash\nfor n in {1..5}\ndo\n    echo \"$n squared is $(( n * n ))\"\ndone\n```\n\n### آرایه\n\n```bash\nnames=(\"ali\" \"mmd\" \"ashkan\")\n\nfor x in \"${names[@]}\"\ndo\n    echo \"name : $x\"\ndone\n```\n\nاشتباهات رایج:\n\n| اشتباه | دلیل |\n| --- | --- |\n| `$names = [...]` | هنگام مقداردهی `$` نمی‌آید و دور `=` فاصله نیست |\n| `[\"a\",\"b\"]` | سینتکس Python و JS است؛ در Bash `[ ]` دستور `test` است |\n| `echo \"$names\"` داخل حلقه | باید متغیر حلقه چاپ شود، نه کل آرایه |\n\n### while\n\n```bash\ncounter=1\nwhile [ $counter -le 5 ]\ndo\n    echo \"number : $counter\"\n    counter=$(( counter + 1 ))\ndone\n```\n\n> نکته: اگر خط افزایش شمارنده را فراموش کنی، شرط هیچ‌وقت نادرست نمی‌شود و حلقه بی‌نهایت اجرا می‌شود. با `Ctrl+C` متوقفش کن.\n\n### while read\n\nخواندن خط‌به‌خط از stdin یا فایل.\n\n```bash\nwhile read line\ndo\n    echo \"line : $line\"\ndone < users.txt\n```\n\nترکیب با pipe:\n\n```bash\ncut -d \":\" -f 1 /etc/passwd | while read user\ndo\n    echo \"user : $user\"\ndone\n```\n\n---\n\n## ۱۲. find\n\n```bash\nfind . -name \"*.txt\"              # جست‌وجو در پوشه‌ی جاری\nfind /etc -name \"passwd\"          # جست‌وجو در مسیر مشخص\nfind . -type f                    # فقط فایل‌ها\nfind . -type d                    # فقط پوشه‌ها\nfind . -name \"*.txt\" | wc -l      # شمارش نتایج\nfind / -name \"passwd\" 2>/dev/null # حذف خطاهای دسترسی\n```\n\n| فلگ | کاربرد |\n| --- | --- |\n| `-name` | تطبیق نام |\n| `-iname` | تطبیق نام بدون حساسیت به حروف |\n| `-type f` / `-type d` | فایل یا پوشه |\n| `-size` | بر اساس حجم |\n| `-mtime` | بر اساس زمان تغییر |\n\n> نکته: الگو را داخل کوتیشن بگذار. اگر `find . -name *.txt` بنویسی، شل قبل از اجرا `*` را باز می‌کند و نتیجه اشتباه می‌شود.\n\n---\n\n## ۱۳. xargs\n\nبرخی برنامه‌ها فقط آرگومان می‌پذیرند و stdin نمی‌خوانند. `xargs` ورودی استاندارد را به آرگومان تبدیل می‌کند.\n\n```bash\necho \"google.com\" | dig            # کار نمی‌کند\necho \"google.com\" | xargs dig      # کار می‌کند\n```\n\n| فلگ | کاربرد |\n| --- | --- |\n| `-n 1` | هر بار یک آرگومان |\n| `-n 2` | هر بار دو آرگومان |\n| `-I {}` | جایگذاری در محل دلخواه |\n| `-0` | همراه با `find -print0` برای نام‌های دارای فاصله |\n\n```bash\nseq 1 5 | xargs echo              # 1 2 3 4 5\nseq 1 5 | xargs -n 1 echo         # هر عدد در یک خط\n\necho \"ali reza\" | xargs -I {} touch \"{}.txt\"\n\nfind . -name \"*.txt\" | wc -l          # تعداد فایل‌ها\nfind . -name \"*.txt\" | xargs wc -l    # تعداد خطوط داخل فایل‌ها\n```\n\n---\n\n## ۱۴. Web Server محلی\n\n```bash\nsudo python3 -m http.server 80                 # روی همه‌ی اینترفیس‌ها\nsudo python3 -m http.server -b 127.0.0.1 80    # فقط لوکال\npython3 -m http.server 8000 &                  # در پس‌زمینه\n```\n\n| حالت | معنی |\n| --- | --- |\n| بدون `-b` | پورت روی تمام شبکه باز است |\n| `-b 127.0.0.1` | فقط از خود ماشین قابل دسترسی است |\n\n> نکته: پورت‌های زیر ۱۰۲۴ نیاز به `sudo` دارند.\n> نکته: قبل از باز کردن یک پورت روی اینترنت، مطمئن شو محتوای پوشه‌ی جاری حساس نیست؛ سرور تمام فایل‌های آن مسیر را سرو می‌کند.\n\n---\n\n## ۱۵. tmux\n\nابزار مدیریت نشست ترمینال. اگر ارتباط SSH قطع شود، پروسه‌ها زنده می‌مانند.\n\n```bash\ntmux                  # نشست جدید\ntmux new -s work      # نشست با نام\ntmux ls               # فهرست نشست‌ها\ntmux attach -t work   # بازگشت به نشست\n```\n\n| کلید | کار |\n| --- | --- |\n| `Ctrl+b` سپس `d` | جدا شدن از نشست |\n| `Ctrl+b` سپس `c` | پنجره‌ی جدید |\n| `Ctrl+b` سپس `n` | پنجره‌ی بعدی |\n| `Ctrl+b` سپس `%` | تقسیم عمودی |\n| `Ctrl+b` سپس `\"` | تقسیم افقی |\n\n---\n\n## ۱۶. مفاهیم Exfiltration\n\nاین بخش مفهومی است و صرفاً برای درک مسیرهای خروج داده در تمرین‌های امنیتی آورده شده است.\n\n| روش | ایده |\n| --- | --- |\n| HTTP Exfiltration | داده در یک درخواست HTTP به سرور مقصد ارسال می‌شود |\n| DNS Exfiltration | داده داخل نام دامنه قرار می‌گیرد و از طریق کوئری DNS خارج می‌شود |\n\nالگوی کلی پردازش خروجی:\n\n```text\nstring → extract → regex\nstring → object → select\n```\n\n```bash\ncurl -s http://example.com | grep \"title\"\ncurl -s http://example.com/api | jq '.data'\nid | base64\necho \"data\" | md5sum\n```\n\n> نکته: این تکنیک‌ها فقط در محیط تمرینی و روی سیستم‌هایی که مجوز آزمایش داری قابل استفاده‌اند.\n\n---\n\n## ۱۷. ابزارهای کمکی\n\n### bat\n\nجایگزین رنگی `cat` با شماره‌ی خط.\n\n```bash\nsudo apt update && sudo apt install bat    # Debian / Ubuntu\nsudo dnf install bat                       # Fedora\nsudo pacman -S bat                         # Arch\n```\n\nدر Debian و Ubuntu نام باینری `batcat` است. برای ساختن نام کوتاه:\n\n```bash\nmkdir -p ~/.local/bin\nln -sf \"$(command -v batcat)\" ~/.local/bin/bat\nexport PATH=\"$HOME/.local/bin:$PATH\"\n```\n\n```bash\nbat script.sh\nbat -n script.sh          # با شماره‌ی خط\nbat --paging=never f.txt  # بدون صفحه‌بندی\n```\n\n---\n\n## ۱۸. جدول مرجع سریع\n\n### جریان‌ها\n\n| شماره | نام |\n| --- | --- |\n| `0` | stdin |\n| `1` | stdout |\n| `2` | stderr |\n\n### هدایت\n\n| نماد | کار |\n| --- | --- |\n| `>` | نوشتن stdout با بازنویسی |\n| `>>` | افزودن stdout |\n| `<` | خواندن ورودی از فایل |\n| `2>` | نوشتن stderr |\n| `2>&1` | ادغام stderr در stdout |\n| `&>` | هر دو خروجی |\n| `\\|` | اتصال دو دستور |\n\n### عملگرها\n\n| نماد | شرط اجرا |\n| --- | --- |\n| `;` | همیشه |\n| `&&` | موفقیت دستور قبل |\n| `\\|\\|` | شکست دستور قبل |\n| `&` | پس‌زمینه |\n\n### مجوزها\n\n| عدد | رشته |\n| --- | --- |\n| `755` | `rwxr-xr-x` |\n| `750` | `rwxr-x---` |\n| `700` | `rwx------` |\n| `654` | `rw-r-xr--` |\n| `644` | `rw-r--r--` |\n| `600` | `rw-------` |\n\n### پرانتزها\n\n| نوشتار | کار |\n| --- | --- |\n| `$( )` | خروجی دستور |\n| `$(( ))` | محاسبه‌ی ریاضی |\n| `( )` | subshell |\n| `[ ]` | دستور `test` |\n",
    "readingTime": "۲۱ دقیقه مطالعه",
    "updatedAt": 1785481801644
  }
];
